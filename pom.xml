<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>net.riftbreaker</groupId>
  <artifactId>rifttowny-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>RiftTowny</name>
  <description>
    Town, nation, territory, economy, governance and war platform for Paper and Folia.
    Clean-room: behaviourally compatible with familiar town plugins, sharing no code with them.
  </description>

  <modules>
    <module>rifttowny-api</module>
    <module>rifttowny-domain</module>
    <module>rifttowny-storage</module>
    <module>rifttowny-integrations</module>
    <module>rifttowny-paper</module>
  </modules>

  <properties>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Pinned rather than the [26.2.build,) range some sibling repositories still use.
         A range re-resolves on every build and silently moves the compile target. -->
    <paper.version>26.2.build.92-stable</paper.version>
    <adventure.version>4.26.0</adventure.version>
    <hikari.version>7.0.2</hikari.version>
    <flyway.version>12.4.0</flyway.version>
    <mariadb.version>3.5.8</mariadb.version>
    <sqlite.version>3.51.1.0</sqlite.version>
    <snakeyaml.version>2.5</snakeyaml.version>
    <junit.version>5.13.4</junit.version>
    <assertj.version>3.27.3</assertj.version>
  </properties>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>
    <repository>
      <id>placeholderapi</id>
      <url>https://repo.helpch.at/releases/</url>
    </repository>
  </repositories>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>io.papermc.paper</groupId>
        <artifactId>paper-api</artifactId>
        <version>${paper.version}</version>
        <scope>provided</scope>
      </dependency>

      <dependency>
        <groupId>net.riftbreaker</groupId>
        <artifactId>rifttowny-api</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>net.riftbreaker</groupId>
        <artifactId>rifttowny-domain</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>net.riftbreaker</groupId>
        <artifactId>rifttowny-storage</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>net.riftbreaker</groupId>
        <artifactId>rifttowny-integrations</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>com.zaxxer</groupId>
        <artifactId>HikariCP</artifactId>
        <version>${hikari.version}</version>
      </dependency>
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-core</artifactId>
        <version>${flyway.version}</version>
      </dependency>
      <dependency>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-mysql</artifactId>
        <version>${flyway.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mariadb.jdbc</groupId>
        <artifactId>mariadb-java-client</artifactId>
        <version>${mariadb.version}</version>
      </dependency>
      <dependency>
        <groupId>org.xerial</groupId>
        <artifactId>sqlite-jdbc</artifactId>
        <version>${sqlite.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.platform</groupId>
      <artifactId>junit-platform-launcher</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>${assertj.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <!-- 3.6.2 or newer: relocation rewrites bytecode and the ASM bundled with
               earlier releases cannot read Java 25 class files. -->
          <version>3.6.2</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.5.0</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.0</version>
        <configuration>
          <release>25</release>
          <compilerArgs>
            <arg>-Xlint:all</arg>
            <arg>-Werror</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.3</version>
        <configuration>
          <useModulePath>false</useModulePath>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <!-- Publishes to GitHub Packages so sibling repositories can resolve rifttowny-api
       instead of building it from source. The id must match the <server> entry Jenkins
       injects via its settings file. -->
  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub RiftbreakerMC Packages</name>
      <url>https://maven.pkg.github.com/RiftbreakerMC/RiftTowny</url>
    </repository>
    <snapshotRepository>
      <id>github</id>
      <name>GitHub RiftbreakerMC Packages</name>
      <url>https://maven.pkg.github.com/RiftbreakerMC/RiftTowny</url>
    </snapshotRepository>
  </distributionManagement>

</project>
